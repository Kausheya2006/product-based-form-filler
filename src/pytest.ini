[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
markers =
    interface: UI/interface flow tests
    auth: authentication and authorization flows
    profile: profile management flows
    forms: form lifecycle flows
    e2e: browser end-to-end tests (opt-in)
